# SERVICEPRO — MIGRATION JOURNAL CLEANUP
# Create missing migration file + sync Drizzle journal
# ONE TASK. MIGRATIONS FOLDER + SCHEMA ONLY.

IGNORE ALL PRIOR SESSION CONTEXT. READ ONLY THIS MESSAGE.

---

## WHAT IS BROKEN

Drizzle's migration journal (`migrations/meta/_journal.json`) only tracks 2 migrations (idx 0 and idx 1). But the database actually has all columns from migrations 0002–0005 applied as raw SQL, plus 3 new columns (`business_city`, `business_state`, `business_zip_prefix`) added directly via ALTER TABLE in a recent fixpack task.

The schema file (`shared/schema.ts`) is the source of truth. The database matches it. The journal does not. This means `drizzle-kit generate` will try to re-create things that already exist, causing the "table-rename ambiguity" conflict.

---

## VERIFICATION — RUN BEFORE TOUCHING ANYTHING

```bash
# Check current journal state
cat migrations/meta/_journal.json

# Check what migration files exist
ls -la migrations/*.sql

# Check that the 3 new columns exist in the live DB
psql $DATABASE_URL -c "\d business_settings" 2>/dev/null | grep -E "business_city|business_state|business_zip"
# OR if psql not available:
node -e "
const { Pool } = require('@neondatabase/serverless');
const { neonConfig } = require('@neondatabase/serverless');
const ws = require('ws');
neonConfig.webSocketConstructor = ws;
const pool = new Pool({ connectionString: process.env.DATABASE_URL });
pool.query(\"SELECT column_name FROM information_schema.columns WHERE table_name = 'business_settings' AND column_name IN ('business_city','business_state','business_zip_prefix')\").then(r => { console.log(r.rows); pool.end(); });
"

# Confirm the 3 columns are in shared/schema.ts
grep -n "businessCity\|businessState\|businessZipPrefix" shared/schema.ts
```

Confirm before writing anything:
- [ ] Journal shows only 2 entries (or fewer than 6)
- [ ] 6 SQL files exist in migrations/
- [ ] 3 new columns ARE in the live database
- [ ] 3 new columns ARE in shared/schema.ts

---

## STEP 1 — Create the missing migration SQL file

Create `migrations/0006_fixpack_business_address_columns.sql`:

```sql
-- Fixpack Task 1: Add tenant address columns for geocoding bias
-- Applied directly in fixpack (bypassed drizzle-kit due to journal conflict)
-- This file documents what was applied and ensures fresh environments work

ALTER TABLE "business_settings" 
  ADD COLUMN IF NOT EXISTS "business_city" text,
  ADD COLUMN IF NOT EXISTS "business_state" text,
  ADD COLUMN IF NOT EXISTS "business_zip_prefix" text;

-- Seed Clean Machine's row (idempotent)
UPDATE "business_settings" 
SET 
  business_city = COALESCE(business_city, 'Tulsa'),
  business_state = COALESCE(business_state, 'OK'),
  business_zip_prefix = COALESCE(business_zip_prefix, '74')
WHERE tenant_id = 'root';
```

---

## STEP 2 — Update the Drizzle journal

The journal needs to acknowledge migrations 0002–0005 so drizzle-kit stops seeing them as unapplied changes. Add entries for each missing migration to `migrations/meta/_journal.json`.

Read the current journal. It will look like:
```json
{
  "version": "7",
  "dialect": "postgresql",
  "entries": [
    { "idx": 0, "version": "7", "when": 1763876223707, "tag": "0000_aromatic_caretaker", "breakpoints": true },
    { "idx": 1, "version": "7", "when": 1763940971585, "tag": "0001_aspiring_cerebro", "breakpoints": true }
  ]
}
```

Add entries for all missing migrations. Use timestamps that are sequentially after idx 1. The exact timestamp doesn't matter as long as they increase. Add one entry per missing file:

```json
{ "idx": 2, "version": "7", "when": 1764100000000, "tag": "0002_portal_settings_columns", "breakpoints": true },
{ "idx": 3, "version": "7", "when": 1764200000000, "tag": "0003_audit_t1_business_settings", "breakpoints": true },
{ "idx": 4, "version": "7", "when": 1764300000000, "tag": "0004_audit_t1_customer_threads", "breakpoints": true },
{ "idx": 5, "version": "7", "when": 1764400000000, "tag": "0005_audit_t2_web_chat_identities", "breakpoints": true },
{ "idx": 6, "version": "7", "when": 1764500000000, "tag": "0006_fixpack_business_address_columns", "breakpoints": true }
```

---

## STEP 3 — Verify drizzle-kit no longer conflicts

```bash
npx drizzle-kit generate --name=verify_sync 2>&1 | head -30
```

If it generates an empty migration (0 changes detected) or only shows the 3 new columns as "already applied" — the journal is in sync. Delete the verify migration file if it was created empty.

If it still shows conflicts, report exactly what it says without running `push` or applying anything.

---

## COMPLETION REPORT

```
## MIGRATION CLEANUP COMPLETION REPORT

### Verification
Journal entries before fix: [number]
SQL migration files that existed: [list]
3 new columns in live DB confirmed: [Y/N]
3 new columns in schema.ts confirmed: [Y/N]

### Changes made
0006_fixpack_business_address_columns.sql created: [Y/N]
Journal entries added (list each tag added):
  - [tag]
  - [tag]
  [etc.]

### Drizzle sync check
drizzle-kit generate result after fix: [empty migration / N changes / conflicts — describe]
Journal now in sync: [Y/N]

### Unresolved issues
[Anything found but not fixable in scope]
```