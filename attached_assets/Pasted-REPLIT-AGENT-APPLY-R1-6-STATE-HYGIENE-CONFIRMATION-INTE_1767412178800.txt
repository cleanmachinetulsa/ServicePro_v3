REPLIT AGENT — APPLY R1.6 “STATE HYGIENE + CONFIRMATION INTEGRITY + MANUAL BOOKING ROUTE”

Goal:
Fix 4 critical issues found in live screenshots:
1) stale draft offers allowing “YES” to confirm an old/past time (e.g., Dec 22)
2) needsHumanAttention not pausing automation (customer still gets “You’re all set” later)
3) unsafe memory hydration pulling/using wrong address (e.g., Minneapolis) + false “within service area”
4) admin manual booking 404: Cannot POST /api/appointments/create-manual

Hard rules:
- NEVER confirm a booking unless there is a booking record AND it has a calendar eventId.
- NEVER accept “YES” as time confirmation unless an offer was sent recently (TTL) AND the offered startTime is in the future.
- If conversation.needsHumanAttention is true OR automation_paused_until is in the future: DO NOT run AI/booking logic. Reply with a short human-followup message only.
- Hydration must be tenant-scoped and exact phone match. If address looks out-of-area or inconsistent, DO NOT auto-use; ask to confirm address.

A) DB MIGRATION (SAFE, NON-DESTRUCTIVE)
Add columns to conversations for offer arming + pausing:
- booking_offer_armed_at TIMESTAMPTZ NULL
- booking_offer_expires_at TIMESTAMPTZ NULL
- booking_offer_payload_hash TEXT NULL
- automation_paused_until TIMESTAMPTZ NULL

Implement using CREATE TABLE/ALTER TABLE ADD COLUMN IF NOT EXISTS style (whichever migration system exists).
If you have drizzle migrations, add a new migration; otherwise add a scripts/sql/xxxx_r1_6.sql and ensure it runs in deploy boot.

B) BOOKING DRAFT / OFFER EXPIRY LOGIC
File: server/services/bookingDraftService.ts (or the file where draft state is persisted/loaded)
Implement helper normalizeDraftOnLoad(conversation, now):
- If conversation.booking_offer_expires_at < now OR booking_offer_armed_at is null → clear offer fields (armed/expires/hash)
- If draft has a selectedStartTime in the past → clear selectedStartTime and any “confirmed” flags
- If draft stage is “awaiting_time_confirm” but there’s no active offer armed → revert stage to “needs_time” (or equivalent)

Also implement armOffer(conversationId, offerHash, ttlMinutes=30):
- sets booking_offer_armed_at = now
- sets booking_offer_expires_at = now + ttl
- sets booking_offer_payload_hash = offerHash

When sending slots to customer, call armOffer() with a deterministic hash of the offered slot list.

C) INBOUND HANDLER: PAUSE AUTOMATION WHEN ESCALATED
File: server/routes/twilioTestSms.ts (primary inbound handler)
At the very top after loading conversation:
- If needsHumanAttention === true OR (automation_paused_until && automation_paused_until > now):
    - DO NOT call AI / bookingDraftService
    - Reply: “Thanks — a human will follow up shortly. If urgent, call us at {businessPhone}.”
    - Return.

Also: whenever escalationService.escalateSmsToHuman() is called, set:
- needsHumanAttention = true
- automation_paused_until = now + 6 hours (configurable)

D) “YES” ACCEPTANCE MUST REQUIRE A LIVE OFFER
In the part that interprets user replies:
- If user says “yes/yeah/yep/ok” and we are treating it as time acceptance:
    - require booking_offer_armed_at != null AND booking_offer_expires_at > now
    - require offeredSlotStartTime >= now + 15 minutes
    - otherwise DO NOT accept it as confirmation; respond asking them to pick a time (re-offer slots).

E) SAFE CUSTOMER MEMORY HYDRATION
File: server/services/smsCustomerMemoryHydrator.ts
Ensure hydration queries include tenant_id AND exact E.164 phone match.
Add safety checks:
- If hydrated address is not in the same state/city service region (or distance check fails / > max) → do NOT auto-fill; mark address as “needs_confirm”
- Never claim “within service area” unless distance check succeeded.
- If no address confirmed in this thread, ask for address before offering slots.

Also remove any generic “I don’t store data / each session independent” language from the agent prompt builder if present:
File: server/ai/smsAgentPromptBuilder.ts
Add a hard platform rule: “Never say you don’t store data; we keep details to help with booking and support.”

F) CONFIRMATION MESSAGE MUST COME FROM BOOKING RECORD
Find any place that sends “You’re all set!” / “Confirmed”:
- Ensure it only happens after retrieving booking record with eventId != null.
- Use booking.startAt from booking record for formatting (single source of truth).
- If missing eventId: do NOT send confirmation; escalate + send pending message.

G) FIX ADMIN MANUAL BOOKING ENDPOINT 404
Search for frontend call:
- `POST /api/appointments/create-manual`
Implement that backend route to match (or change frontend to existing route, but keep backwards compatibility).
Server route requirements:
- Auth required (tenant admin)
- Tenant-scoped
- Creates appointment/booking + calendar eventId, returns JSON with bookingId + eventId
- On calendar failure: return 409/500 with clear message and set needsHumanAttention for that conversation if applicable

Files likely:
- server/routes.ts (register route)
- server/routes/appointments.ts or server/routes/bookings.ts if those exist

H) VERIFICATION STEPS (PRINT THESE IN AGENT OUTPUT)
After changes:
1) Stale YES test:
   - start a booking, get slot list, WAIT 35 minutes (or manually set expires_at in DB to past), then reply “Yes”
   - expected: system does NOT book; it re-offers slots / asks to pick a time
2) CALFAIL ON test:
   - #TEST CALFAIL ON then book
   - expected: owner escalation + customer pending message
   - expected: NO later “You’re all set” message
3) Unroutable test:
   - #TEST UNROUTABLE ON then “Hi”
   - expected: owner alert + short routability message; no AI flow
4) Manual booking:
   - admin Create Appointment button should succeed (no 404)

Make sure code compiles and tests pass.
Return a short summary of files changed and the exact route name implemented for manual booking.
