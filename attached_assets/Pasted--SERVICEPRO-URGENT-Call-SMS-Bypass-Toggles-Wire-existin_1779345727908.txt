# SERVICEPRO — URGENT: Call & SMS Bypass Toggles
# Wire existing FORWARD_ALL_CALLS mode to UI + add global SMS bypass
# PRIORITY: OWNER IS LOSING CUSTOMERS RIGHT NOW

IGNORE ALL PRIOR SESSION CONTEXT. READ ONLY THIS MESSAGE.

---

## WHAT THIS DOES

Adds two emergency toggles in Phone Settings that let the owner immediately redirect all inbound calls and/or SMS to their personal phone — bypassing the AI entirely. The call forwarding backend already exists and just needs UI wiring. SMS bypass needs a new global setting.

## CLEAN MACHINE SAFETY

Both toggles default to OFF. No existing behavior changes unless the owner explicitly activates them. All changes are additive.

---

## VERIFICATION — RUN BEFORE WRITING ANY CODE

```bash
# Confirm FORWARD_ALL_CALLS backend exists
grep -n "FORWARD_ALL_CALLS\|handleForwardAllCalls\|forwardingNumber" server/routes.twilioVoiceCanonical.ts | head -10

# Confirm forwardingNumber column exists in tenantPhoneConfig
grep -n "forwardingNumber\|telephonyMode" shared/schema.ts | head -10

# Read PhoneSettings.tsx to understand current UI structure
wc -l client/src/components/PhoneSettings.tsx
grep -n "telephonyMode\|forwardingNumber\|TelephonyMode\|mode.*select\|select.*mode" client/src/components/PhoneSettings.tsx | head -20

# Read useTelephonySettings hook fully
cat client/src/hooks/useTelephonySettings.ts

# Check if businessSettings has a global SMS bypass column
grep -n "bypassAi\|bypass_ai\|smsForward\|globalPause\|smsMode\|smsBypass" shared/schema.ts | head -5

# Check what the phone config update route looks like
grep -rn "telephonyMode\|forwardingNumber" server/ | grep -v ".test.\|node_modules" | grep "route\|PUT\|POST\|PATCH\|update" | head -10
```

Confirm before writing:
- [ ] `FORWARD_ALL_CALLS` case exists in `routes.twilioVoiceCanonical.ts` and uses `phoneConfig.forwardingNumber`
- [ ] `telephonyMode` column exists in `tenantPhoneConfig` with `FORWARD_ALL_CALLS` as a valid value
- [ ] `forwardingNumber` column exists in `tenantPhoneConfig`
- [ ] `PhoneSettings.tsx` already has a forwarding number input — find the exact component structure
- [ ] No global SMS bypass column exists in `businessSettings` — you will add one

---

## TASK A — Wire call forwarding mode selector in `PhoneSettings.tsx`

The forwarding number input already exists. What's missing is a **telephony mode selector** that lets the owner switch between modes.

Find where the forwarding number input is rendered in `PhoneSettings.tsx`. Add a mode selector immediately above it:

```tsx
{/* Telephony Mode — Emergency Override */}
<div className="space-y-3 rounded-lg border border-amber-200 bg-amber-50 dark:bg-amber-950/20 dark:border-amber-800 p-4">
  <div className="flex items-center gap-2">
    <span className="text-sm font-semibold text-amber-900 dark:text-amber-200">
      📞 Call Routing Mode
    </span>
  </div>

  <div className="flex flex-col gap-2">
    {[
      { value: 'AI_FIRST', label: 'AI answers first', description: 'AI greets caller, offers options, transfers to you if needed' },
      { value: 'FORWARD_ALL_CALLS', label: '🔴 Forward all calls to my phone', description: 'Skip AI — every call rings your forwarding number directly' },
      { value: 'AI_ONLY', label: 'AI only', description: 'AI handles everything, no live transfer' },
    ].map((option) => (
      <label key={option.value} className="flex items-start gap-3 cursor-pointer">
        <input
          type="radio"
          name="telephonyMode"
          value={option.value}
          checked={localLine.telephonyMode === option.value}
          onChange={() => handleChange('telephonyMode', option.value)}
          className="mt-0.5"
        />
        <div>
          <div className="text-sm font-medium">{option.label}</div>
          <div className="text-xs text-muted-foreground">{option.description}</div>
        </div>
      </label>
    ))}
  </div>

  {/* Only show forwarding number input when FORWARD_ALL_CALLS is selected */}
  {localLine.telephonyMode === 'FORWARD_ALL_CALLS' && (
    <div className="mt-2 space-y-1">
      <label className="text-xs font-medium text-amber-900 dark:text-amber-200">
        Forward to phone number
      </label>
      <input
        type="tel"
        placeholder="+19185551234"
        className="w-full rounded border px-3 py-1.5 text-sm"
        value={localLine.forwardingNumber || ''}
        onChange={(e) => handleChange('forwardingNumber', e.target.value)}
      />
      <p className="text-xs text-muted-foreground">
        Enter in E.164 format (+1XXXXXXXXXX). This is your personal cell or any number that should ring.
      </p>
      {!localLine.forwardingNumber && (
        <p className="text-xs text-amber-600 font-medium">
          ⚠ Enter a forwarding number before saving, or calls will fall back to AI mode.
        </p>
      )}
    </div>
  )}
</div>
```

Make sure `telephonyMode` is included in the update payload when saving. Read the existing `handleSave` or `handleChange` pattern and add `telephonyMode` alongside `forwardingNumber`.

Verify the save hits the correct backend route and that `telephonyMode` is actually persisted. Check the route handler accepts `telephonyMode` in its update logic.

---

## TASK B — Add global SMS bypass to `businessSettings`

### B1 — Add column to `shared/schema.ts`

Find the `businessSettings` table. Add one nullable boolean column:

```typescript
smsForwardingEnabled: boolean("sms_forwarding_enabled").default(false),
```

### B2 — Apply migration (direct SQL — do not use drizzle-kit push)

```sql
ALTER TABLE "business_settings" 
  ADD COLUMN IF NOT EXISTS "sms_forwarding_enabled" boolean DEFAULT false;
```

Run this directly. Document it in the completion report.

### B3 — Add bypass check in `server/routes/twilioTestSms.ts`

Find the section near the top of the SMS handler where `tenantDb` is available and `businessSettings` has been loaded (check how settings are loaded — there may be a `getBusinessSettings(tenantDb)` call or similar pattern).

After settings are loaded, add:

```typescript
// EMERGENCY SMS BYPASS: If owner has enabled SMS forwarding, 
// notify them and skip AI — do not reply to customer automatically
if (settings?.smsForwardingEnabled) {
  const ownerPhone = settings?.alertPhone || process.env.BUSINESS_OWNER_PERSONAL_PHONE;
  if (ownerPhone) {
    // Send owner a notification SMS with the customer's message
    try {
      const { sendSms } = await import('../services/smsSendService'); // find correct import
      await sendSms({
        to: ownerPhone,
        body: `[SMS BYPASS] From ${From}: ${Body}`,
        tenantId,
        tenantDb,
      });
    } catch (notifyErr) {
      console.warn('[SMS BYPASS] Owner notify failed:', notifyErr);
    }
  }
  // Return empty TwiML — no AI reply
  const bypassResponse = new MessagingResponse();
  res.type('text/xml');
  return res.send(bypassResponse.toString());
}
```

**Important:** Read the actual pattern for sending SMS in this codebase before writing the import — find how outbound SMS is sent in other parts of the file and use the same pattern. Do not invent a new service.

### B4 — Add SMS bypass toggle to `PhoneSettings.tsx`

Add below the call routing mode selector:

```tsx
{/* SMS Bypass Toggle */}
<div className="flex items-center justify-between rounded-lg border border-amber-200 bg-amber-50 dark:bg-amber-950/20 dark:border-amber-800 p-4">
  <div>
    <div className="text-sm font-semibold text-amber-900 dark:text-amber-200">
      💬 SMS Bypass Mode
    </div>
    <div className="text-xs text-muted-foreground mt-0.5">
      Skip AI for all incoming texts — you'll get a notification SMS with each message
    </div>
    {smsForwardingEnabled && (
      <div className="text-xs text-amber-600 font-medium mt-1">
        🔴 ACTIVE — AI is bypassed, texts are forwarded to {settings?.alertPhone || 'your alert phone'}
      </div>
    )}
  </div>
  <Switch
    checked={smsForwardingEnabled}
    onCheckedChange={handleSmsBypassToggle}
  />
</div>
```

Wire `handleSmsBypassToggle` to save `smsForwardingEnabled` to `businessSettings` via a PATCH/PUT to the settings endpoint. Read how other settings toggles in this component save their values and use the same pattern.

---

## IMPORTANT NOTES FOR REPLIT

1. **Read before writing.** The save patterns in `PhoneSettings.tsx` are already established — follow them exactly for both new fields.
2. **Find the actual outbound SMS function** used in `twilioTestSms.ts` before writing the bypass notification. Do not create a new one.
3. **The forwarding number input may already be rendered** — do not duplicate it. Find it, then add the mode selector above it.
4. **TypeScript must compile clean** — run `npx tsc --noEmit` before finishing.
5. **Do not touch** `routes.twilioVoiceCanonical.ts` — the backend for calls is already correct.

---

## TESTS

```bash
npx tsc --noEmit
npx vitest run server/tests/smsSendGuard.test.ts
```

---

## COMPLETION REPORT

```
## CALL & SMS BYPASS COMPLETION REPORT

### Call forwarding UI
PhoneSettings.tsx — mode selector added: [Y/N]
telephonyMode saved in update payload: [Y/N]
forwardingNumber shown only when FORWARD_ALL_CALLS selected: [Y/N]
Backend route confirmed to accept telephonyMode: [Y/N]
TypeScript compile: [clean/errors]

### SMS bypass
sms_forwarding_enabled column added to schema: [Y/N]
SQL ALTER TABLE applied: [Y/N]
Bypass check location in twilioTestSms.ts: [approx line]
Outbound SMS function used for owner notification: [function name used]
Toggle UI added to PhoneSettings.tsx: [Y/N]
Toggle saves to businessSettings: [Y/N]

### Tests
smsSendGuard: [pass/fail]
TypeScript: [clean/errors]

### To activate right now (instructions for owner)
1. Go to Settings → Phone & SMS
2. Set Call Routing Mode to "Forward all calls to my phone"  
3. Enter your personal cell in E.164 format (+1XXXXXXXXXX)
4. Enable SMS Bypass Mode toggle
5. Save
6. Test by calling/texting your Twilio number

### Unresolved issues
[Anything found but not fixable in scope]
```