# SERVICEPRO — FIXPACK TASK 4 ONLY
# Fix validateAddress to use tenant service area, not Clean Machine's
# ONE TASK. THREE FILES. NOTHING ELSE.

IGNORE ALL PRIOR SESSION CONTEXT. READ ONLY THIS MESSAGE.

---

## YOUR ONLY JOB IN THIS SESSION

`validateAddress()` in `server/schedulingTools.ts` currently calls `checkDistanceToBusinessLocation(address)` with no `tenantDb`. This triggers the HOTFIX-SMS-CM fallback which uses Clean Machine's Tulsa coordinates for every tenant. Thread `tenantDb` through so each tenant validates against their own service area.

Touching:
1. `server/schedulingTools.ts` — add `tenantDb` param, pass it through
2. `server/openai.ts` — update the `case "validate_address"` call site
3. `server/conversationalScheduling.ts` — DEFERRED (explained below, do not touch)

---

## CLEAN MACHINE SAFETY

After this change, Clean Machine's `validateAddress` calls use their own `business_settings` row — their Tulsa lat/lng — the same coordinates as today. The HOTFIX-SMS-CM fallback still exists as a safety net for the deferred call site.

---

## VERIFICATION — RUN THESE BEFORE TOUCHING ANY FILE

```bash
# Confirm validateAddress current signature (no tenantDb param)
grep -n "export async function validateAddress" server/schedulingTools.ts

# Confirm the checkDistanceToBusinessLocation call with no tenantDb
sed -n '155,180p' server/schedulingTools.ts

# Confirm no TenantDb import in schedulingTools.ts currently
head -15 server/schedulingTools.ts

# Find ALL validateAddress call sites
grep -rn "validateAddress(" server/

# Check executeFunctionCall signature — confirm tenantId?: string is a param
sed -n '645,660p' server/openai.ts

# Confirm the validate_address case in openai.ts
grep -n "case.*validate_address" server/openai.ts
sed -n '673,682p' server/openai.ts

# Confirm wrapTenantDb export
grep -n "export.*wrapTenantDb" server/tenantDb.ts

# Check handleConversationalScheduling signature — confirm NO tenantId param
grep -n "export async function handleConversationalScheduling" server/conversationalScheduling.ts
sed -n '5,12p' server/conversationalScheduling.ts
```

Confirm before writing code:
- [ ] `validateAddress` currently has NO `tenantDb` parameter
- [ ] `schedulingTools.ts` has NO `TenantDb` import currently
- [ ] `executeFunctionCall` in `openai.ts` receives `tenantId?: string`
- [ ] `wrapTenantDb(db, tenantId)` is the correct export pattern from `tenantDb.ts`
- [ ] `handleConversationalScheduling` has NO `tenantId` param → DEFERRED
- [ ] Exact number of `validateAddress` call sites (handle all of them)

---

## CHANGE 1 — `server/schedulingTools.ts`

**1a — Add TenantDb import at the top (check it doesn't already exist first):**

```typescript
import type { TenantDb } from './tenantDb';
```

**1b — Add `tenantDb` as optional third parameter to `validateAddress`:**

```typescript
// Before:
export async function validateAddress(
  phone: string,
  address: string | undefined | null,
): Promise<AddressValidationResult>

// After:
export async function validateAddress(
  phone: string,
  address: string | undefined | null,
  tenantDb?: TenantDb,
): Promise<AddressValidationResult>
```

**1c — Pass `tenantDb` to `checkDistanceToBusinessLocation`:**

```typescript
// Before:
const result = await checkDistanceToBusinessLocation(address);

// After:
const result = tenantDb
  ? await checkDistanceToBusinessLocation(tenantDb, address)
  : await checkDistanceToBusinessLocation(address); // HOTFIX-SMS-CM fallback preserved
```

**1d — Update the internal call site in `schedulingTools.ts` (~line 637, inside `updateAppointmentAddress`):**

`updateAppointmentAddress(phone, newAddress)` does not have `tenantDb` in scope. Leave this call as-is — it uses the legacy fallback. Note it in the completion report as a secondary deferred fix.

---

## CHANGE 2 — `server/openai.ts` — `case "validate_address"`

`executeFunctionCall` receives `tenantId?: string`. Construct `tenantDb` from it and pass to `validateAddress`:

```typescript
case "validate_address": {
  let tenantDbForValidation: import('./tenantDb').TenantDb | undefined;
  if (tenantId) {
    const { db } = await import('./db');
    const { wrapTenantDb } = await import('./tenantDb');
    tenantDbForValidation = wrapTenantDb(db, tenantId);
  }
  const result = await validateAddress(args.phone, args.address, tenantDbForValidation);
  return JSON.stringify(result);
}
```

Verify the `wrapTenantDb(db, tenantId)` call matches the actual export signature from `server/tenantDb.ts` before using it. If it differs, follow the repo pattern and note the difference.

---

## DEFERRED — `server/conversationalScheduling.ts` (~line 110)

`handleConversationalScheduling` has no `tenantId` parameter. Threading it requires changing that function's signature and all its callers — out of scope here. The legacy fallback protects this path until it's fixed in a later task. Document in the completion report.

---

## TESTS

```bash
npx vitest run server/tests/tenantCommRouter.test.ts
npx vitest run server/tests/smsSendGuard.test.ts
```

---

## COMPLETION REPORT

```
## TASK 4 COMPLETION REPORT

### Verification
validateAddress had no tenantDb param before: [Y/N]
schedulingTools.ts had no TenantDb import before: [Y/N]
executeFunctionCall tenantId?: string confirmed: [Y/N]
wrapTenantDb pattern confirmed from tenantDb.ts: [exact call used]
handleConversationalScheduling has no tenantId: [Y/N — DEFERRED confirmed]
Total validateAddress call sites found: [number, list them]

### Changes made
TenantDb import added to schedulingTools.ts: [Y/N]
validateAddress signature updated with tenantDb?: TenantDb: [Y/N]
checkDistanceToBusinessLocation receives tenantDb when available: [Y/N]
openai.ts case "validate_address" updated: [Y/N — line number]
wrapTenantDb call matches repo pattern: [Y/N]

### Deferred call sites
conversationalScheduling.ts (~line 110): [DEFERRED — no tenantId in scope]
schedulingTools.ts updateAppointmentAddress (~line 637): [DEFERRED — no tenantId in scope]

### Tests
tenantCommRouter: [pass/fail]
smsSendGuard: [pass/fail]

### Unresolved issues
[Anything found but not fixable in scope]
```