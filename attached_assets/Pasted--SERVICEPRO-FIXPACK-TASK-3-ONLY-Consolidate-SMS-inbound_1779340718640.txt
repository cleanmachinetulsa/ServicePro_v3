# SERVICEPRO — FIXPACK TASK 3 ONLY
# Consolidate SMS inbound dedup to one atomic gate
# ONE TASK. TWO FILES. NOTHING ELSE.

IGNORE ALL PRIOR SESSION CONTEXT. READ ONLY THIS MESSAGE.

---

## YOUR ONLY JOB IN THIS SESSION

Remove the second, redundant dedup check in `server/routes/twilioTestSms.ts`. The correct gate already exists at line ~244. The redundant select-first check at line ~1435 creates a race window. Remove it.

Touching: `server/routes/twilioTestSms.ts`, possibly `server/services/smsInboundDedup.ts` if the insert pattern needs fixing.

---

## CLEAN MACHINE SAFETY

The existing gate at line ~244 already uses `'root'` as the tenantId. The uniqueness constraint on `sms_inbound_dedup` is on `messageSid` globally — so this works correctly for all tenants. That gate stays exactly as-is. You are only removing the second check.

---

## VERIFICATION — RUN THESE BEFORE TOUCHING ANY FILE

```bash
# Find both dedup call sites
grep -n "isDuplicateInboundSms\|recordProcessedInboundSms" server/routes/twilioTestSms.ts

# Check the import line
sed -n '11p' server/routes/twilioTestSms.ts

# Read the existing correct gate (around line 244)
sed -n '238,258p' server/routes/twilioTestSms.ts

# Read the redundant check (around line 1435)
sed -n '1428,1445p' server/routes/twilioTestSms.ts

# Confirm smsInboundDedup uses atomic insert-on-conflict
grep -n "INSERT\|insert\|onConflict\|CONFLICT\|returning\|select" server/services/smsInboundDedup.ts
```

Confirm before writing code:
- [ ] Exact line of the correct gate (`recordProcessedInboundSms`)
- [ ] Exact line of the redundant check (`isDuplicateInboundSms`)
- [ ] `smsInboundDedup.ts` uses `INSERT ... ON CONFLICT DO NOTHING` (not select-then-insert)
- [ ] The correct gate runs BEFORE any LLM, booking, or conversation work

---

## CHANGE 1 — `server/services/smsInboundDedup.ts` (only if broken)

Read the file. If `recordProcessedInboundSms` already uses atomic `insert().onConflictDoNothing().returning(...)` and returns `true` for new / `false` for conflict — it is correct, do not touch it.

If it uses a select-then-insert pattern instead, fix it to:
```typescript
const rows = await db
  .insert(smsInboundDedup)
  .values({ messageSid, fromPhone, toPhone, tenantId })
  .onConflictDoNothing()
  .returning({ id: smsInboundDedup.id });
return rows.length > 0; // true = new, false = duplicate
```

---

## CHANGE 2 — `server/routes/twilioTestSms.ts`

**2a — Remove `isDuplicateInboundSms` from the import at line 11:**

```typescript
// Before:
import { isDuplicateInboundSms, recordProcessedInboundSms } from '../services/smsInboundDedup';

// After:
import { recordProcessedInboundSms } from '../services/smsInboundDedup';
```

**2b — Find and remove the `isDuplicateInboundSms` call at line ~1435:**

Find the block that looks like:
```typescript
const isDuplicate = await isDuplicateInboundSms(messageSid);
if (isDuplicate) {
  // ... early return or skip logic
}
```

Delete this entire block. The gate at line ~244 already handles this correctly.

**Result:** Exactly one dedup call remains in the file — `recordProcessedInboundSms` at line ~244.

---

## VERIFY THE RESULT

```bash
# Must show exactly ONE result (the correct gate, not the removed one)
grep -n "isDuplicateInboundSms\|recordProcessedInboundSms" server/routes/twilioTestSms.ts
```

Expected output: one line showing `recordProcessedInboundSms` at ~line 244. Zero lines for `isDuplicateInboundSms`.

---

## TESTS

```bash
npx vitest run server/tests/smsSendGuard.test.ts
npx vitest run server/tests/tenantCommRouter.test.ts
```

---

## COMPLETION REPORT

```
## TASK 3 COMPLETION REPORT

### Verification
Correct gate (recordProcessedInboundSms) location: [line number]
Redundant check (isDuplicateInboundSms) location before removal: [line number]
smsInboundDedup.ts pattern: [atomic insert-on-conflict / select-then-insert — fixed]
Correct gate runs before LLM/booking work: [Y/N]

### Changes made
smsInboundDedup.ts changed: [Y/N — if Y, describe what changed]
isDuplicateInboundSms removed from import: [Y/N]
isDuplicateInboundSms call removed from line: [line number]

### Final state
grep result after removal — lines with recordProcessedInboundSms: [1]
grep result after removal — lines with isDuplicateInboundSms: [0]

### Tests
smsSendGuard: [pass/fail]
tenantCommRouter: [pass/fail]

### Unresolved issues
[Anything found but not fixable in scope]
```